#!/usr/bin/env bash

cat << EOF > ${HOME}/.gitconfig.local
[user]
      name = manastria
      email = manastria@users.noreply.github.com
EOF


cat << EOF > ${HOME}/.github_config
username = manastria
EOF
